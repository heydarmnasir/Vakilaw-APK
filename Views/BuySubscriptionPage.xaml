<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Vakilaw.Views.BuySubscriptionPage"
             Title="BuySubscriptionPage">
    <VerticalStackLayout Padding="20">
        <Label Text="انتخاب اشتراک" FontSize="20" HorizontalOptions="Center"/>
        <Button Text="۳ ماهه" Command="{Binding Buy3MonthCommand}" />
        <Button Text="۶ ماهه" Command="{Binding Buy6MonthCommand}" />
        <Button Text="۱۲ ماهه" Command="{Binding BuyYearlyCommand}" />
    </VerticalStackLayout>
</ContentPage>